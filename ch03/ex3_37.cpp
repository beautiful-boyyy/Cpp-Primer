#include <bits/stdc++.h>
using namespace std;

int main()
{
    const char ca[] = {'h', 'e', 'l', 'l', 'o'};
    const char *cp = ca;
    while (*cp)
    {
        cout << *cp << endl;
        cp ++;
    }
    const int size = 10;
    int array[size];
    return 0;
}
